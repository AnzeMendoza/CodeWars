#include <stdio.h>

enum ESTADO {CERRADO, ABIERTO, CERRANDO, ABRIENDO};

int main()
{
    printf("cerrado : %i\n", CERRADO);
    printf("cerrado : %i\n", ABIERTO);
    printf("cerrado : %i\n", CERRANDO);
    printf("cerrado : %i\n", ABRIENDO);
    return 0;
}
